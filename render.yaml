services:
  - type: web
    name: afric-connect
    runtime: node
    plan: free
    rootDir: projet BTP
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 24